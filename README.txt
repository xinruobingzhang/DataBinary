#################

Auto Binary Data
Databinary.R code file
Data.binary(data,form)

Arguments:

	data           the input data
	form           a formula object.An offset term can be included.The offset causes fitting of a model 
                       such as logit(Y=1)=X¦Â+W,where W is the offset variable having no estimated coefficient.
	               The response variable can be any data type.
        

Value

        binarydata     already binary data